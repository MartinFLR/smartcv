import { Component } from '@angular/core';
import {FormsModule} from '@angular/forms';

@Component({
  selector: 'app-config',
  imports: [
    FormsModule,
  ],
  templateUrl: './config.html',
  styleUrl: './config.css',
})
export class Config {

}
